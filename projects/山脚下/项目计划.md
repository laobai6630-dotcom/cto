# 山脚下项目计划书 v2.0

> **版本**: v2.0  
> **创建日期**: 2025-12-17  
> **状态**: 规划完成，待启动  
> **核心改动**: 数据保留180日 | 特征窗口20日 | 对照组功能

---

## 📑 目录导航

- [第一部分：项目概述](#第一部分项目概述)
  - [1.1 项目灵感与背景](#11-项目灵感与背景)
  - [1.2 项目目标](#12-项目目标)
  - [1.3 实施计划](#13-实施计划)
- [第二部分：核心改动](#第二部分核心改动)
  - [2.1 九项核心改动汇总](#21-九项核心改动汇总)
  - [2.2 改动影响分析](#22-改动影响分析)
- [第三部分：完整系统架构](#第三部分完整系统架构)
  - [3.1 六层目录结构](#31-六层目录结构)
  - [3.2 数据架构设计](#32-数据架构设计)
  - [3.3 数据流向说明](#33-数据流向说明)
- [第四部分：88特征体系](#第四部分88特征体系)
  - [4.1 特征分类](#41-特征分类)
  - [4.2 特征权重配置](#42-特征权重配置)
  - [4.3 特征工程实现](#43-特征工程实现)
- [第五部分：实施路线图](#第五部分实施路线图)
  - [5.1 员工配置与权限](#51-员工配置与权限)
  - [5.2 四阶段实施计划](#52-四阶段实施计划)
  - [5.3 对照组功能实现](#53-对照组功能实现)
  - [5.4 快速参考](#54-快速参考)

---

## 第一部分：项目概述

### 1.1 项目灵感与背景

**项目名称**: 山脚下 (Shanjiaxia)

**灵感来源**:
- "山脚下"寓意着从基础做起，脚踏实地
- 代表着从最基层的数据出发，逐步构建完整的预测系统
- 象征着持续攀登、不断优化的过程

**业务背景**:
本项目旨在构建一个基于机器学习的客户流失预测系统，通过分析客户的历史行为数据，预测客户在未来7天内的流失风险，帮助企业提前采取挽留措施。

**核心价值**:
1. **数据驱动决策**: 基于180天的历史数据进行分析和预测
2. **精准预测**: 使用88个精心设计的特征，提高预测准确率
3. **实时响应**: 每日更新预测结果，及时发现高风险客户
4. **科学验证**: 引入对照组机制，验证挽留措施的实际效果
5. **灵活部署**: 支持本地和云端多种部署方式

### 1.2 项目目标

#### 业务目标
- **降低客户流失率**: 目标降低15-20%
- **提升挽留成功率**: 通过精准识别和及时干预，提升挽留成功率至40%以上
- **优化运营成本**: 减少无效挽留投入，聚焦高价值客户
- **数据资产积累**: 建立完整的客户行为数据仓库

#### 技术目标
- **预测准确率**: AUC ≥ 0.75，准确率 ≥ 70%
- **系统响应时间**: 预测任务完成时间 < 30分钟
- **数据处理能力**: 支持10万+客户的日常分析
- **系统稳定性**: 可用性 ≥ 99%

#### 团队目标
- **知识传承**: 建立完整的文档体系和知识库
- **技能提升**: 团队成员掌握机器学习实战技能
- **流程规范**: 建立标准化的开发、测试、部署流程

### 1.3 实施计划

#### 项目周期
- **总周期**: 4个阶段，约120天
- **第一阶段**: 环境搭建与数据准备 (30天) ✅
- **第二阶段**: 特征工程与模型训练 (30天)
- **第三阶段**: 系统集成与测试 (30天)
- **第四阶段**: 部署上线与优化 (30天)

#### 资源配置
- **人员**: 4人团队（详见5.1节）
- **硬件**: 本地服务器或云服务器
- **预算**: 根据实际情况调整

---

## 第二部分：核心改动

### 2.1 九项核心改动汇总

| 序号 | 改动项 | v1.0 | v2.0 | 影响范围 | 优先级 |
|------|--------|------|------|----------|--------|
| 1 | **数据保留期** | 90天 | 180天 | 数据库存储、特征计算 | ⭐⭐⭐ |
| 2 | **特征窗口期** | 30天 | 20天 | 特征工程、模型训练 | ⭐⭐⭐ |
| 3 | **对照组功能** | 无 | 新增 | 系统设计、数据库表 | ⭐⭐⭐ |
| 4 | **预测周期** | 7天 | 7天 | 保持不变 | - |
| 5 | **特征数量** | 基础版 | 88个完整特征 | 特征工程 | ⭐⭐ |
| 6 | **模型类型** | 单模型 | 支持多模型对比 | 模型训练模块 | ⭐⭐ |
| 7 | **部署方式** | 本地 | 本地+云端 | 部署架构 | ⭐ |
| 8 | **监控告警** | 基础 | 完整监控体系 | 运维模块 | ⭐⭐ |
| 9 | **文档体系** | 简单 | 完整知识库 | 团队协作 | ⭐⭐ |

### 2.2 改动影响分析

#### 2.2.1 数据保留期：90天 → 180天

**改动原因**:
- 捕捉更长期的客户行为模式
- 支持季节性分析
- 提供更充足的训练数据

**影响分析**:
```
✅ 优势:
- 增加数据样本量，提升模型泛化能力
- 支持长期趋势分析
- 可以观察客户生命周期的更多阶段

⚠️ 挑战:
- 存储空间增加约1倍
- 数据清洗和处理时间增加
- 需要考虑数据质量的时效性
```

**实施要点**:
1. 数据库设计时预留足够存储空间
2. 建立数据归档和清理机制
3. 定期检查历史数据质量
4. 优化查询性能，避免慢查询

#### 2.2.2 特征窗口期：30天 → 20天

**改动原因**:
- 近期行为对流失预测更有预测力
- 减少特征计算复杂度
- 平衡预测准确性和时效性

**影响分析**:
```
✅ 优势:
- 特征计算更快
- 对近期行为变化更敏感
- 减少数据噪声影响

⚠️ 注意事项:
- 可能错过30天外的重要模式
- 需要通过实验验证效果
- 调整特征工程代码
```

**实施要点**:
1. 所有时间窗口特征统一调整为20天
2. 保留部分长期特征（如总体趋势）
3. 通过A/B测试验证效果
4. 更新文档和配置文件

#### 2.2.3 对照组功能（新增）

**功能说明**:
对照组机制用于科学评估挽留措施的实际效果，通过对比干预组和对照组的流失率差异，验证预测模型和挽留策略的价值。

**核心设计**:
```
高风险客户识别
     ↓
随机分组（9:1）
     ↓
┌─────────┴─────────┐
│                   │
干预组(90%)       对照组(10%)
    ↓                  ↓
执行挽留措施        不执行挽留
    ↓                  ↓
观察7天              观察7天
    ↓                  ↓
    └─────────┬─────────┘
              ↓
        对比分析效果
```

**数据库设计**:
```sql
-- 对照组配置表
CREATE TABLE control_group_config (
    id SERIAL PRIMARY KEY,
    experiment_name VARCHAR(100) NOT NULL,
    start_date DATE NOT NULL,
    end_date DATE,
    control_ratio DECIMAL(3,2) DEFAULT 0.10,
    status VARCHAR(20) DEFAULT 'active',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 对照组分配表
CREATE TABLE control_group_assignments (
    id SERIAL PRIMARY KEY,
    customer_id VARCHAR(50) NOT NULL,
    experiment_id INTEGER REFERENCES control_group_config(id),
    group_type VARCHAR(20) NOT NULL, -- 'control' or 'treatment'
    assigned_date DATE NOT NULL,
    risk_score DECIMAL(5,4),
    outcome VARCHAR(20), -- 'churned', 'retained', 'unknown'
    outcome_date DATE,
    UNIQUE(customer_id, experiment_id)
);

-- 对照组效果分析表
CREATE TABLE control_group_analysis (
    id SERIAL PRIMARY KEY,
    experiment_id INTEGER REFERENCES control_group_config(id),
    analysis_date DATE NOT NULL,
    control_group_size INTEGER,
    control_group_churn_rate DECIMAL(5,4),
    treatment_group_size INTEGER,
    treatment_group_churn_rate DECIMAL(5,4),
    absolute_difference DECIMAL(5,4),
    relative_difference DECIMAL(5,4),
    statistical_significance BOOLEAN,
    p_value DECIMAL(6,5)
);
```

**实施流程**:
1. **配置实验**: 设定对照组比例、实验周期
2. **随机分组**: 对高风险客户进行随机分配
3. **执行干预**: 仅对干预组执行挽留措施
4. **观察跟踪**: 记录两组客户的实际流失情况
5. **效果分析**: 计算差异并进行统计检验
6. **结果报告**: 生成实验报告和优化建议

---

## 第三部分：完整系统架构

### 3.1 六层目录结构

```
shanjiaxia/                          # ✅ 项目根目录
│
├── config/                          # ✅ 配置文件目录
│   ├── database.yaml               # 数据库连接配置
│   ├── features.yaml               # 特征工程配置
│   ├── model.yaml                  # 模型参数配置
│   ├── logging.yaml                # 日志配置
│   └── control_group.yaml          # 对照组配置
│
├── data/                           # ✅ 数据目录
│   ├── raw/                        # 原始数据
│   │   ├── customers.csv           # 客户基础信息
│   │   ├── transactions.csv        # 交易记录
│   │   └── interactions.csv        # 客户互动记录
│   ├── processed/                  # 处理后的数据
│   │   ├── features.csv            # 特征数据
│   │   └── labels.csv              # 标签数据
│   ├── models/                     # 模型文件
│   │   ├── best_model.pkl          # 最佳模型
│   │   └── model_history/          # 历史模型版本
│   └── predictions/                # 预测结果
│       └── daily/                  # 每日预测输出
│
├── src/                            # ✅ 源代码目录
│   ├── data_processing/            # 数据处理模块
│   │   ├── __init__.py
│   │   ├── data_loader.py          # 数据加载
│   │   ├── data_cleaner.py         # 数据清洗
│   │   └── data_validator.py       # 数据验证
│   │
│   ├── feature_engineering/        # 特征工程模块
│   │   ├── __init__.py
│   │   ├── base_features.py        # 基础特征
│   │   ├── transaction_features.py # 交易特征
│   │   ├── behavior_features.py    # 行为特征
│   │   ├── rfm_features.py         # RFM特征
│   │   └── advanced_features.py    # 高级特征
│   │
│   ├── modeling/                   # 模型训练模块
│   │   ├── __init__.py
│   │   ├── train.py                # 模型训练
│   │   ├── evaluate.py             # 模型评估
│   │   ├── predict.py              # 模型预测
│   │   └── model_registry.py       # 模型注册
│   │
│   ├── control_group/              # 对照组模块 (新增)
│   │   ├── __init__.py
│   │   ├── assignment.py           # 分组分配
│   │   ├── tracking.py             # 跟踪观察
│   │   └── analysis.py             # 效果分析
│   │
│   ├── api/                        # API接口模块
│   │   ├── __init__.py
│   │   ├── app.py                  # Flask/FastAPI应用
│   │   └── endpoints.py            # API端点定义
│   │
│   └── utils/                      # 工具函数
│       ├── __init__.py
│       ├── db_utils.py             # 数据库工具
│       ├── logger.py               # 日志工具
│       └── metrics.py              # 指标计算
│
├── scripts/                        # ✅ 脚本目录
│   ├── setup_database.py           # 数据库初始化
│   ├── daily_prediction.py         # 每日预测任务
│   ├── model_retraining.py         # 模型重训练
│   └── generate_reports.py         # 报告生成
│
├── tests/                          # ✅ 测试目录
│   ├── unit/                       # 单元测试
│   ├── integration/                # 集成测试
│   └── test_data/                  # 测试数据
│
├── docs/                           # ✅ 文档目录
│   ├── architecture.md             # 架构说明
│   ├── api_reference.md            # API文档
│   ├── feature_guide.md            # 特征说明
│   ├── deployment.md               # 部署指南
│   └── troubleshooting.md          # 故障排查
│
├── notebooks/                      # Jupyter笔记本
│   ├── eda.ipynb                   # 探索性数据分析
│   ├── feature_analysis.ipynb      # 特征分析
│   └── model_experiments.ipynb     # 模型实验
│
├── logs/                           # 日志目录
│   ├── app.log                     # 应用日志
│   ├── training.log                # 训练日志
│   └── prediction.log              # 预测日志
│
├── requirements.txt                # Python依赖
├── setup.py                        # 安装脚本
├── .gitignore                      # Git忽略配置
├── README.md                       # 项目说明
└── LICENSE                         # 许可证
```

### 3.2 数据架构设计

#### 3.2.1 数据库表结构

**客户基础信息表** (customers)
```sql
CREATE TABLE customers (
    customer_id VARCHAR(50) PRIMARY KEY,
    registration_date DATE NOT NULL,
    customer_type VARCHAR(20),
    region VARCHAR(50),
    age_group VARCHAR(20),
    gender VARCHAR(10),
    vip_level INTEGER DEFAULT 0,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

**交易记录表** (transactions)
```sql
CREATE TABLE transactions (
    transaction_id VARCHAR(50) PRIMARY KEY,
    customer_id VARCHAR(50) REFERENCES customers(customer_id),
    transaction_date DATE NOT NULL,
    amount DECIMAL(10,2) NOT NULL,
    product_category VARCHAR(50),
    payment_method VARCHAR(20),
    status VARCHAR(20),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_transactions_customer ON transactions(customer_id);
CREATE INDEX idx_transactions_date ON transactions(transaction_date);
```

**客户互动表** (interactions)
```sql
CREATE TABLE interactions (
    interaction_id VARCHAR(50) PRIMARY KEY,
    customer_id VARCHAR(50) REFERENCES customers(customer_id),
    interaction_date DATE NOT NULL,
    interaction_type VARCHAR(50), -- 'login', 'browse', 'support', 'complaint'
    duration_seconds INTEGER,
    channel VARCHAR(20), -- 'web', 'mobile', 'phone', 'email'
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_interactions_customer ON interactions(customer_id);
CREATE INDEX idx_interactions_date ON interactions(interaction_date);
```

**预测结果表** (predictions)
```sql
CREATE TABLE predictions (
    prediction_id SERIAL PRIMARY KEY,
    customer_id VARCHAR(50) REFERENCES customers(customer_id),
    prediction_date DATE NOT NULL,
    churn_probability DECIMAL(5,4) NOT NULL,
    risk_level VARCHAR(20), -- 'low', 'medium', 'high', 'critical'
    model_version VARCHAR(20),
    feature_importance JSONB,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(customer_id, prediction_date)
);

CREATE INDEX idx_predictions_date ON predictions(prediction_date);
CREATE INDEX idx_predictions_risk ON predictions(risk_level);
```

**特征数据表** (features)
```sql
CREATE TABLE features (
    feature_id SERIAL PRIMARY KEY,
    customer_id VARCHAR(50) REFERENCES customers(customer_id),
    calculation_date DATE NOT NULL,
    feature_data JSONB NOT NULL, -- 存储所有88个特征
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(customer_id, calculation_date)
);

CREATE INDEX idx_features_date ON features(calculation_date);
CREATE INDEX idx_features_jsonb ON features USING gin(feature_data);
```

**模型训练记录表** (model_training_log)
```sql
CREATE TABLE model_training_log (
    training_id SERIAL PRIMARY KEY,
    model_version VARCHAR(20) NOT NULL,
    training_date TIMESTAMP NOT NULL,
    train_start_date DATE,
    train_end_date DATE,
    train_samples INTEGER,
    validation_samples INTEGER,
    model_type VARCHAR(50),
    hyperparameters JSONB,
    metrics JSONB, -- AUC, accuracy, precision, recall, F1
    feature_importance JSONB,
    model_path VARCHAR(255),
    status VARCHAR(20),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### 3.2.2 数据保留策略

**180天数据保留策略**:
```python
# 数据保留配置
DATA_RETENTION_CONFIG = {
    'transactions': {
        'retention_days': 180,
        'archive_enabled': True,
        'archive_path': 'data/archive/transactions/'
    },
    'interactions': {
        'retention_days': 180,
        'archive_enabled': True,
        'archive_path': 'data/archive/interactions/'
    },
    'features': {
        'retention_days': 180,
        'archive_enabled': True,
        'archive_path': 'data/archive/features/'
    },
    'predictions': {
        'retention_days': 365,  # 预测结果保留更久用于回溯分析
        'archive_enabled': True,
        'archive_path': 'data/archive/predictions/'
    }
}

# 数据清理脚本
def cleanup_old_data(table_name, retention_days):
    """清理超过保留期的数据"""
    cutoff_date = datetime.now() - timedelta(days=retention_days)
    
    # 归档数据
    if DATA_RETENTION_CONFIG[table_name]['archive_enabled']:
        archive_data(table_name, cutoff_date)
    
    # 删除数据
    query = f"""
        DELETE FROM {table_name}
        WHERE created_at < %s
    """
    execute_query(query, (cutoff_date,))
```

### 3.3 数据流向说明

```
┌─────────────────┐
│  数据源系统      │
│ (业务数据库)     │
└────────┬────────┘
         │ ETL抽取
         ↓
┌─────────────────┐
│  原始数据层      │
│ (raw data)      │
│ - customers.csv │
│ - transactions  │
│ - interactions  │
└────────┬────────┘
         │ 数据清洗
         ↓
┌─────────────────┐
│  清洗数据层      │
│ (cleaned data)  │
│ - 去重          │
│ - 异常值处理     │
│ - 缺失值填充     │
└────────┬────────┘
         │ 特征计算 (20天窗口)
         ↓
┌─────────────────┐
│  特征数据层      │
│ (features)      │
│ - 88个特征      │
│ - 特征版本管理   │
└────────┬────────┘
         │ 模型训练/预测
         ↓
┌─────────────────┐
│  预测结果层      │
│ (predictions)   │
│ - 流失概率      │
│ - 风险等级      │
└────────┬────────┘
         │ 分组决策
         ↓
┌─────────────────┐
│  对照组分配      │
│ (control group) │
│ - 干预组 90%    │
│ - 对照组 10%    │
└────────┬────────┘
         │
    ┌────┴────┐
    │         │
    ↓         ↓
┌──────┐  ┌──────┐
│干预组│  │对照组│
│执行  │  │观察  │
│挽留  │  │不干预│
└──┬───┘  └───┬──┘
   │          │
   └────┬─────┘
        │ 效果分析
        ↓
┌─────────────────┐
│  分析报告层      │
│ (reports)       │
│ - 效果对比      │
│ - 优化建议      │
└─────────────────┘
```

---

## 第四部分：88特征体系

### 4.1 特征分类

88个特征按照业务逻辑分为8大类：

#### 4.1.1 基础信息特征 (8个)
```python
BASIC_FEATURES = {
    1: 'customer_age_days',           # 客户注册天数
    2: 'customer_type',                # 客户类型
    3: 'region',                       # 所在地区
    4: 'age_group',                    # 年龄段
    5: 'gender',                       # 性别
    6: 'vip_level',                    # VIP等级
    7: 'account_status',               # 账户状态
    8: 'registration_channel'          # 注册渠道
}
```

#### 4.1.2 RFM特征 (12个)
```python
RFM_FEATURES = {
    # Recency - 最近一次消费时间
    9: 'days_since_last_transaction',  # 距离最后交易天数
    10: 'days_since_last_login',       # 距离最后登录天数
    11: 'recency_score',               # 新近度得分 (1-5)
    
    # Frequency - 消费频次
    12: 'transaction_count_20d',       # 20天内交易次数
    13: 'login_count_20d',             # 20天内登录次数
    14: 'interaction_count_20d',       # 20天内互动次数
    15: 'frequency_score',             # 频率得分 (1-5)
    
    # Monetary - 消费金额
    16: 'total_amount_20d',            # 20天内总消费金额
    17: 'avg_transaction_amount_20d',  # 20天内平均交易金额
    18: 'max_transaction_amount_20d',  # 20天内最大交易金额
    19: 'monetary_score',              # 价值得分 (1-5)
    20: 'rfm_combined_score'           # RFM综合得分
}
```

#### 4.1.3 交易行为特征 (20个)
```python
TRANSACTION_FEATURES = {
    # 金额相关
    21: 'total_amount_20d',            # 20天总金额
    22: 'avg_amount_20d',              # 20天平均金额
    23: 'std_amount_20d',              # 20天金额标准差
    24: 'min_amount_20d',              # 20天最小金额
    25: 'max_amount_20d',              # 20天最大金额
    
    # 频次相关
    26: 'transaction_count_20d',       # 20天交易次数
    27: 'avg_daily_transactions_20d',  # 20天日均交易次数
    28: 'transaction_frequency_trend', # 交易频次趋势
    
    # 品类相关
    29: 'unique_categories_20d',       # 20天购买品类数
    30: 'top_category',                # 最常购买品类
    31: 'category_diversity_20d',      # 品类多样性
    
    # 支付相关
    32: 'payment_method_count',        # 使用支付方式数
    33: 'primary_payment_method',      # 主要支付方式
    
    # 时间模式
    34: 'avg_transaction_interval',    # 平均交易间隔
    35: 'transaction_regularity',      # 交易规律性
    36: 'weekend_transaction_ratio',   # 周末交易占比
    37: 'evening_transaction_ratio',   # 晚间交易占比
    
    # 趋势特征
    38: 'amount_trend_20d',            # 金额趋势
    39: 'frequency_trend_20d',         # 频次趋势
    40: 'recent_vs_historical_ratio'   # 近期vs历史对比
}
```

#### 4.1.4 互动行为特征 (15个)
```python
INTERACTION_FEATURES = {
    # 登录行为
    41: 'login_count_20d',             # 20天登录次数
    42: 'avg_login_duration_20d',      # 20天平均登录时长
    43: 'login_frequency_trend',       # 登录频次趋势
    44: 'days_with_login_20d',         # 20天内有登录的天数
    
    # 浏览行为
    45: 'browse_count_20d',            # 20天浏览次数
    46: 'avg_browse_depth_20d',        # 20天平均浏览深度
    47: 'browse_to_purchase_ratio',    # 浏览转化率
    
    # 客服互动
    48: 'support_interaction_count',   # 客服互动次数
    49: 'complaint_count_20d',         # 20天投诉次数
    50: 'avg_response_satisfaction',   # 平均响应满意度
    
    # 渠道偏好
    51: 'primary_channel',             # 主要使用渠道
    52: 'channel_diversity',           # 渠道多样性
    53: 'mobile_usage_ratio',          # 移动端使用占比
    
    # 活跃度
    54: 'active_days_20d',             # 20天活跃天数
    55: 'engagement_score'             # 参与度得分
}
```

#### 4.1.5 变化趋势特征 (12个)
```python
TREND_FEATURES = {
    # 短期趋势 (最近7天 vs 前7天)
    56: 'amount_change_7d',            # 金额变化率
    57: 'frequency_change_7d',         # 频次变化率
    58: 'login_change_7d',             # 登录变化率
    
    # 中期趋势 (最近10天 vs 前10天)
    59: 'amount_change_10d',           # 金额变化率
    60: 'frequency_change_10d',        # 频次变化率
    61: 'engagement_change_10d',       # 活跃度变化率
    
    # 长期趋势 (最近20天 vs 历史平均)
    62: 'amount_vs_avg',               # 金额vs历史均值
    63: 'frequency_vs_avg',            # 频次vs历史均值
    
    # 波动性
    64: 'amount_volatility',           # 金额波动率
    65: 'frequency_volatility',        # 频次波动率
    
    # 衰减趋势
    66: 'activity_decay_rate',         # 活跃度衰减率
    67: 'momentum_score'               # 动量得分
}
```

#### 4.1.6 生命周期特征 (8个)
```python
LIFECYCLE_FEATURES = {
    68: 'customer_lifetime_days',      # 客户生命周期天数
    69: 'lifecycle_stage',             # 生命周期阶段
    70: 'total_lifetime_value',        # 历史总价值
    71: 'avg_monthly_value',           # 月均价值
    72: 'lifetime_transaction_count',  # 历史总交易次数
    73: 'retention_rate_historical',   # 历史留存率
    74: 'churn_risk_history',          # 历史流失风险
    75: 'reactivation_count'           # 重新激活次数
}
```

#### 4.1.7 社交网络特征 (6个)
```python
SOCIAL_FEATURES = {
    76: 'referral_count',              # 推荐用户数
    77: 'referred_by_active_user',     # 是否被活跃用户推荐
    78: 'social_influence_score',      # 社交影响力得分
    79: 'friend_count',                # 好友数量
    80: 'friend_activity_level',       # 好友活跃度
    81: 'network_effect_score'         # 网络效应得分
}
```

#### 4.1.8 风险信号特征 (7个)
```python
RISK_FEATURES = {
    82: 'consecutive_inactive_days',   # 连续不活跃天数
    83: 'sudden_activity_drop',        # 活跃度骤降标志
    84: 'negative_feedback_count',     # 负面反馈次数
    85: 'failed_transaction_count',    # 失败交易次数
    86: 'account_security_issues',     # 账户安全问题
    87: 'competitor_search_signal',    # 竞品搜索信号
    88: 'unsubscribe_signal'           # 退订信号
}
```

### 4.2 特征权重配置

基于业务经验和模型特征重要性分析，各类特征的权重分配：

```python
FEATURE_WEIGHTS = {
    'basic_info': 0.10,        # 基础信息特征
    'rfm': 0.20,               # RFM特征
    'transaction': 0.20,       # 交易行为特征
    'interaction': 0.15,       # 互动行为特征
    'trend': 0.15,             # 变化趋势特征
    'lifecycle': 0.10,         # 生命周期特征
    'social': 0.05,            # 社交网络特征
    'risk': 0.05               # 风险信号特征
}
```

**TOP 20 重要特征**:
```python
TOP_FEATURES = [
    'days_since_last_transaction',     # 最后交易距今天数
    'days_since_last_login',           # 最后登录距今天数
    'consecutive_inactive_days',       # 连续不活跃天数
    'rfm_combined_score',              # RFM综合得分
    'transaction_count_20d',           # 20天交易次数
    'total_amount_20d',                # 20天总金额
    'frequency_change_7d',             # 7天频次变化率
    'amount_change_7d',                # 7天金额变化率
    'login_count_20d',                 # 20天登录次数
    'engagement_score',                # 参与度得分
    'activity_decay_rate',             # 活跃度衰减率
    'sudden_activity_drop',            # 活跃度骤降
    'complaint_count_20d',             # 20天投诉次数
    'transaction_regularity',          # 交易规律性
    'customer_lifetime_days',          # 客户生命周期
    'avg_transaction_amount_20d',      # 20天平均交易金额
    'active_days_20d',                 # 20天活跃天数
    'browse_to_purchase_ratio',        # 浏览转化率
    'negative_feedback_count',         # 负面反馈数
    'monetary_score'                   # 价值得分
]
```

### 4.3 特征工程实现

#### 4.3.1 特征计算框架

```python
# feature_engineering/base.py
from abc import ABC, abstractmethod
import pandas as pd
from datetime import datetime, timedelta

class BaseFeatureCalculator(ABC):
    """特征计算基类"""
    
    def __init__(self, window_days=20, calculation_date=None):
        self.window_days = window_days
        self.calculation_date = calculation_date or datetime.now().date()
        self.window_start = self.calculation_date - timedelta(days=window_days)
    
    @abstractmethod
    def calculate(self, customer_id: str) -> dict:
        """计算特征"""
        pass
    
    def get_window_data(self, table_name: str, customer_id: str) -> pd.DataFrame:
        """获取窗口期内的数据"""
        query = f"""
            SELECT * FROM {table_name}
            WHERE customer_id = %s
            AND date >= %s AND date <= %s
        """
        return pd.read_sql(query, self.db_conn, 
                          params=(customer_id, self.window_start, self.calculation_date))

# feature_engineering/rfm_features.py
class RFMFeatureCalculator(BaseFeatureCalculator):
    """RFM特征计算器"""
    
    def calculate(self, customer_id: str) -> dict:
        features = {}
        
        # 获取交易数据
        transactions = self.get_window_data('transactions', customer_id)
        
        if transactions.empty:
            return self._default_features()
        
        # Recency - 最近性
        last_transaction_date = transactions['transaction_date'].max()
        features['days_since_last_transaction'] = (
            self.calculation_date - last_transaction_date
        ).days
        
        # Frequency - 频率
        features['transaction_count_20d'] = len(transactions)
        features['avg_daily_transactions_20d'] = len(transactions) / self.window_days
        
        # Monetary - 价值
        features['total_amount_20d'] = transactions['amount'].sum()
        features['avg_transaction_amount_20d'] = transactions['amount'].mean()
        features['max_transaction_amount_20d'] = transactions['amount'].max()
        
        # RFM评分
        features['recency_score'] = self._calculate_recency_score(
            features['days_since_last_transaction']
        )
        features['frequency_score'] = self._calculate_frequency_score(
            features['transaction_count_20d']
        )
        features['monetary_score'] = self._calculate_monetary_score(
            features['total_amount_20d']
        )
        features['rfm_combined_score'] = (
            features['recency_score'] * 0.3 +
            features['frequency_score'] * 0.3 +
            features['monetary_score'] * 0.4
        )
        
        return features
    
    def _calculate_recency_score(self, days: int) -> int:
        """计算新近度得分 (1-5, 5最好)"""
        if days <= 3:
            return 5
        elif days <= 7:
            return 4
        elif days <= 14:
            return 3
        elif days <= 20:
            return 2
        else:
            return 1
    
    def _calculate_frequency_score(self, count: int) -> int:
        """计算频率得分 (1-5, 5最好)"""
        if count >= 20:
            return 5
        elif count >= 10:
            return 4
        elif count >= 5:
            return 3
        elif count >= 2:
            return 2
        else:
            return 1
    
    def _calculate_monetary_score(self, amount: float) -> int:
        """计算价值得分 (1-5, 5最好)"""
        # 根据业务实际情况调整阈值
        if amount >= 10000:
            return 5
        elif amount >= 5000:
            return 4
        elif amount >= 1000:
            return 3
        elif amount >= 100:
            return 2
        else:
            return 1

# feature_engineering/feature_pipeline.py
class FeaturePipeline:
    """特征计算流水线"""
    
    def __init__(self, window_days=20):
        self.window_days = window_days
        self.calculators = [
            RFMFeatureCalculator(window_days),
            TransactionFeatureCalculator(window_days),
            InteractionFeatureCalculator(window_days),
            TrendFeatureCalculator(window_days),
            LifecycleFeatureCalculator(window_days),
            SocialFeatureCalculator(window_days),
            RiskFeatureCalculator(window_days)
        ]
    
    def calculate_all_features(self, customer_id: str) -> dict:
        """计算所有88个特征"""
        all_features = {}
        
        for calculator in self.calculators:
            features = calculator.calculate(customer_id)
            all_features.update(features)
        
        # 验证特征完整性
        assert len(all_features) == 88, f"Expected 88 features, got {len(all_features)}"
        
        return all_features
    
    def batch_calculate(self, customer_ids: list) -> pd.DataFrame:
        """批量计算特征"""
        results = []
        for customer_id in customer_ids:
            features = self.calculate_all_features(customer_id)
            features['customer_id'] = customer_id
            results.append(features)
        
        return pd.DataFrame(results)
```

---

## 第五部分：实施路线图

### 5.1 员工配置与权限

#### 5.1.1 团队架构

```
项目经理/Tech Lead (1人)
         │
    ┌────┴────┬────────┬────────┐
    │         │        │        │
数据工程师  算法工程师  后端工程师  测试工程师
  (1人)      (1人)     (1人)     (兼职)
```

#### 5.1.2 岗位职责

**1. 项目经理/Tech Lead**
- **职责**:
  - 项目整体规划和进度管理
  - 技术架构设计和决策
  - 团队协调和资源分配
  - 与业务部门沟通对接
  - 代码审查和质量把控
- **技能要求**:
  - 5年以上项目管理经验
  - 熟悉机器学习项目全流程
  - 具备数据架构设计能力
  - 良好的沟通和领导能力
- **系统权限**: 全部权限

**2. 数据工程师**
- **职责**:
  - 数据库设计和优化
  - ETL流程开发
  - 数据清洗和预处理
  - 特征工程实现
  - 数据质量监控
- **技能要求**:
  - 熟练掌握SQL和Python
  - 熟悉PostgreSQL/MySQL
  - 了解数据仓库概念
  - 有ETL开发经验
- **系统权限**: 数据库读写、脚本执行

**3. 算法工程师**
- **职责**:
  - 模型算法选型和实现
  - 特征分析和优化
  - 模型训练和调优
  - 模型评估和验证
  - 算法文档编写
- **技能要求**:
  - 熟悉机器学习算法
  - 熟练使用scikit-learn/XGBoost等
  - 有流失预测项目经验优先
  - 良好的数学统计基础
- **系统权限**: 模型训练、预测执行

**4. 后端工程师**
- **职责**:
  - API接口开发
  - 系统集成开发
  - 对照组模块开发
  - 定时任务配置
  - 系统监控和运维
- **技能要求**:
  - 熟练掌握Python/Flask或FastAPI
  - 熟悉RESTful API设计
  - 了解Docker和CI/CD
  - 有系统集成经验
- **系统权限**: 代码部署、系统配置

#### 5.1.3 权限管理表

| 功能模块 | 项目经理 | 数据工程师 | 算法工程师 | 后端工程师 |
|---------|---------|-----------|-----------|-----------|
| 数据库访问 | ✅ 完全 | ✅ 读写 | ✅ 只读 | ✅ 只读 |
| 数据清洗 | ✅ | ✅ | ❌ | ❌ |
| 特征工程 | ✅ | ✅ | ✅ | ❌ |
| 模型训练 | ✅ | ❌ | ✅ | ❌ |
| 模型部署 | ✅ | ❌ | ✅ | ✅ |
| API开发 | ✅ | ❌ | ❌ | ✅ |
| 对照组配置 | ✅ | ❌ | ✅ | ✅ |
| 系统配置 | ✅ | ✅ | ❌ | ✅ |
| 生产部署 | ✅ | ❌ | ❌ | ✅ |
| 监控告警 | ✅ | ✅ | ✅ | ✅ |

### 5.2 四阶段实施计划

#### 阶段一：环境搭建与数据准备 (30天) ✅

**目标**: 完成开发环境搭建，建立数据基础

**主要任务**:

**Week 1-2: 环境搭建**
- [ ] 服务器环境准备
  - [ ] Python 3.8+ 环境安装
  - [ ] PostgreSQL 数据库安装配置
  - [ ] Git 版本控制配置
  - [ ] 开发工具安装 (IDE, Jupyter)
- [ ] 项目结构搭建
  - [ ] 创建完整目录结构
  - [ ] 配置文件模板创建
  - [ ] 日志系统配置
  - [ ] Git workflow 设置

**Week 3-4: 数据库设计**
- [ ] 数据库设计
  - [ ] 设计所有表结构
  - [ ] 创建索引和约束
  - [ ] 设置数据保留策略 (180天)
  - [ ] 配置数据归档机制
- [ ] 数据导入
  - [ ] 编写数据导入脚本
  - [ ] 历史数据清洗和导入
  - [ ] 数据质量验证
  - [ ] 建立数据字典

**Week 5: 数据探索**
- [ ] EDA (探索性数据分析)
  - [ ] 数据分布分析
  - [ ] 流失率统计
  - [ ] 缺失值分析
  - [ ] 异常值检测
- [ ] 标签定义
  - [ ] 定义流失标准 (7天未活跃)
  - [ ] 生成训练标签
  - [ ] 标签分布分析

**交付物**:
- ✅ 完整的开发环境
- ✅ 数据库表结构和数据
- ✅ EDA分析报告
- ✅ 数据质量报告

---

#### 阶段二：特征工程与模型训练 (30天)

**目标**: 完成88个特征开发，训练出有效的预测模型

**主要任务**:

**Week 6-7: 基础特征开发**
- [ ] RFM特征实现 (12个)
- [ ] 交易行为特征 (20个)
- [ ] 基础信息特征 (8个)
- [ ] 特征单元测试

**Week 8: 高级特征开发**
- [ ] 互动行为特征 (15个)
- [ ] 变化趋势特征 (12个)
- [ ] 生命周期特征 (8个)
- [ ] 社交网络特征 (6个)
- [ ] 风险信号特征 (7个)

**Week 9: 特征分析**
- [ ] 特征相关性分析
- [ ] 特征重要性评估
- [ ] 特征筛选和优化
- [ ] 特征版本管理

**Week 10: 模型训练**
- [ ] 数据集划分 (训练/验证/测试)
- [ ] 模型选型实验
  - [ ] Logistic Regression (基线)
  - [ ] Random Forest
  - [ ] XGBoost
  - [ ] LightGBM
- [ ] 超参数调优
- [ ] 模型评估和对比
- [ ] 选择最佳模型

**交付物**:
- ✅ 88个特征的计算代码
- ✅ 特征分析报告
- ✅ 训练好的模型文件
- ✅ 模型评估报告

---

#### 阶段三：系统集成与测试 (30天)

**目标**: 完成系统开发，实现自动化预测流程

**主要任务**:

**Week 11-12: 预测系统开发**
- [ ] 每日预测脚本
  - [ ] 自动特征计算
  - [ ] 批量预测
  - [ ] 结果存储
- [ ] 定时任务配置
  - [ ] 使用cron或APScheduler
  - [ ] 任务监控
  - [ ] 失败重试机制

**Week 13: 对照组模块开发** (新增)
- [ ] 对照组数据库表创建
- [ ] 随机分组算法实现
- [ ] 分组规则配置
- [ ] 对照组跟踪系统
- [ ] 效果分析脚本

**Week 14: API开发**
- [ ] 预测API
  - [ ] 单客户预测接口
  - [ ] 批量预测接口
  - [ ] 历史预测查询接口
- [ ] 对照组API
  - [ ] 分组配置接口
  - [ ] 分组查询接口
  - [ ] 效果分析接口
- [ ] 文档自动生成

**Week 15: 测试**
- [ ] 单元测试
- [ ] 集成测试
- [ ] 性能测试
- [ ] 压力测试
- [ ] Bug修复

**交付物**:
- ✅ 完整的预测系统
- ✅ 对照组功能模块
- ✅ API文档
- ✅ 测试报告

---

#### 阶段四：部署上线与优化 (30天)

**目标**: 系统上线运行，持续监控和优化

**主要任务**:

**Week 16: 部署准备**
- [ ] 生产环境配置
- [ ] 数据库迁移
- [ ] 安全加固
- [ ] 备份策略配置

**Week 17: 上线部署**
- [ ] 系统部署
- [ ] 监控配置
  - [ ] 系统监控
  - [ ] 模型监控
  - [ ] 告警配置
- [ ] 日志分析配置

**Week 18: 试运行**
- [ ] 对照组实验启动
- [ ] 第一批预测结果
- [ ] 业务反馈收集
- [ ] 问题修复

**Week 19-20: 效果评估与优化**
- [ ] 对照组效果分析
  - [ ] 流失率对比
  - [ ] 统计检验
  - [ ] ROI计算
- [ ] 模型优化
  - [ ] 根据实际效果调整
  - [ ] 特征优化
  - [ ] 阈值调整
- [ ] 系统优化
  - [ ] 性能优化
  - [ ] 用户体验优化

**交付物**:
- ✅ 线上运行系统
- ✅ 监控大盘
- ✅ 对照组实验报告
- ✅ 优化建议报告

### 5.3 对照组功能实现

#### 5.3.1 对照组设计原则

**科学性原则**:
- 随机分配，避免选择偏差
- 样本量充足，保证统计显著性
- 对照组比例合理 (10%)

**业务可行性原则**:
- 不影响核心业务
- 对照组客户不受损
- 实验周期合理 (7-14天)

**伦理性原则**:
- 保护用户隐私
- 不损害用户利益
- 可随时终止实验

#### 5.3.2 实施流程

```python
# control_group/assignment.py
import random
from datetime import datetime, timedelta

class ControlGroupAssigner:
    """对照组分配器"""
    
    def __init__(self, control_ratio=0.10):
        self.control_ratio = control_ratio
        
    def assign_groups(self, high_risk_customers: list, experiment_name: str) -> dict:
        """
        将高风险客户随机分配到对照组和干预组
        
        Args:
            high_risk_customers: 高风险客户列表
            experiment_name: 实验名称
            
        Returns:
            {
                'control_group': [...],
                'treatment_group': [...],
                'experiment_id': int
            }
        """
        # 创建实验配置
        experiment_id = self._create_experiment(experiment_name)
        
        # 随机打乱
        random.shuffle(high_risk_customers)
        
        # 计算分组数量
        total_count = len(high_risk_customers)
        control_count = int(total_count * self.control_ratio)
        
        # 分组
        control_group = high_risk_customers[:control_count]
        treatment_group = high_risk_customers[control_count:]
        
        # 保存分组结果
        self._save_assignments(
            experiment_id,
            control_group,
            treatment_group
        )
        
        return {
            'experiment_id': experiment_id,
            'control_group': control_group,
            'treatment_group': treatment_group,
            'control_count': len(control_group),
            'treatment_count': len(treatment_group)
        }
    
    def _create_experiment(self, experiment_name: str) -> int:
        """创建实验配置"""
        query = """
            INSERT INTO control_group_config 
            (experiment_name, start_date, control_ratio, status)
            VALUES (%s, %s, %s, %s)
            RETURNING id
        """
        result = execute_query(
            query,
            (experiment_name, datetime.now().date(), self.control_ratio, 'active')
        )
        return result[0]['id']
    
    def _save_assignments(self, experiment_id: int, 
                         control_group: list, treatment_group: list):
        """保存分组分配结果"""
        assignments = []
        
        for customer_id in control_group:
            assignments.append({
                'customer_id': customer_id,
                'experiment_id': experiment_id,
                'group_type': 'control',
                'assigned_date': datetime.now().date()
            })
        
        for customer_id in treatment_group:
            assignments.append({
                'customer_id': customer_id,
                'experiment_id': experiment_id,
                'group_type': 'treatment',
                'assigned_date': datetime.now().date()
            })
        
        # 批量插入
        batch_insert('control_group_assignments', assignments)

# control_group/tracking.py
class ControlGroupTracker:
    """对照组跟踪器"""
    
    def track_outcomes(self, experiment_id: int, tracking_days=7):
        """
        跟踪对照组和干预组的实际流失情况
        
        Args:
            experiment_id: 实验ID
            tracking_days: 跟踪天数 (默认7天)
        """
        # 获取分组信息
        assignments = self._get_assignments(experiment_id)
        
        # 检查每个客户的实际流失情况
        for assignment in assignments:
            customer_id = assignment['customer_id']
            assigned_date = assignment['assigned_date']
            
            # 检查是否流失 (7天内无活跃)
            is_churned = self._check_churn_status(
                customer_id,
                assigned_date,
                tracking_days
            )
            
            # 更新结果
            self._update_outcome(
                assignment['id'],
                'churned' if is_churned else 'retained',
                assigned_date + timedelta(days=tracking_days)
            )
    
    def _check_churn_status(self, customer_id: str, 
                           start_date, tracking_days: int) -> bool:
        """检查客户是否流失"""
        end_date = start_date + timedelta(days=tracking_days)
        
        # 查询跟踪期内是否有活跃行为
        query = """
            SELECT COUNT(*) as activity_count
            FROM (
                SELECT customer_id FROM transactions 
                WHERE customer_id = %s 
                AND transaction_date BETWEEN %s AND %s
                UNION
                SELECT customer_id FROM interactions
                WHERE customer_id = %s
                AND interaction_date BETWEEN %s AND %s
            ) as activities
        """
        result = execute_query(
            query,
            (customer_id, start_date, end_date, customer_id, start_date, end_date)
        )
        
        return result[0]['activity_count'] == 0

# control_group/analysis.py
class ControlGroupAnalyzer:
    """对照组分析器"""
    
    def analyze_experiment(self, experiment_id: int) -> dict:
        """
        分析对照组实验效果
        
        Returns:
            {
                'control_group_churn_rate': float,
                'treatment_group_churn_rate': float,
                'absolute_difference': float,
                'relative_difference': float,
                'statistical_significance': bool,
                'p_value': float
            }
        """
        # 获取结果数据
        results = self._get_experiment_results(experiment_id)
        
        # 计算流失率
        control_churn_rate = self._calculate_churn_rate(
            results['control_group']
        )
        treatment_churn_rate = self._calculate_churn_rate(
            results['treatment_group']
        )
        
        # 计算差异
        absolute_diff = control_churn_rate - treatment_churn_rate
        relative_diff = absolute_diff / control_churn_rate if control_churn_rate > 0 else 0
        
        # 统计检验 (卡方检验)
        from scipy.stats import chi2_contingency
        
        contingency_table = [
            [results['control_churned'], results['control_retained']],
            [results['treatment_churned'], results['treatment_retained']]
        ]
        chi2, p_value, dof, expected = chi2_contingency(contingency_table)
        
        is_significant = p_value < 0.05
        
        # 保存分析结果
        self._save_analysis(
            experiment_id,
            control_churn_rate,
            treatment_churn_rate,
            absolute_diff,
            relative_diff,
            is_significant,
            p_value
        )
        
        return {
            'control_group_churn_rate': control_churn_rate,
            'treatment_group_churn_rate': treatment_churn_rate,
            'absolute_difference': absolute_diff,
            'relative_difference': relative_diff,
            'statistical_significance': is_significant,
            'p_value': p_value,
            'interpretation': self._interpret_results(
                absolute_diff, relative_diff, is_significant
            )
        }
    
    def _interpret_results(self, absolute_diff: float, 
                          relative_diff: float, is_significant: bool) -> str:
        """解释实验结果"""
        if not is_significant:
            return "实验结果未达到统计显著性，无法得出明确结论。建议扩大样本量或延长实验周期。"
        
        if absolute_diff > 0:
            return (
                f"挽留措施有效！干预组流失率比对照组低 {absolute_diff:.2%} "
                f"(相对降低 {relative_diff:.2%})，差异具有统计显著性。"
            )
        else:
            return "挽留措施未显示出明显效果，建议优化挽留策略。"
```

### 5.4 快速参考

#### 5.4.1 关键命令

```bash
# 环境配置
python -m venv venv
source venv/bin/activate  # Linux/Mac
pip install -r requirements.txt

# 数据库初始化
python scripts/setup_database.py

# 每日预测任务
python scripts/daily_prediction.py --date 2025-12-17

# 模型重训练
python scripts/model_retraining.py --window-days 20

# 对照组分配
python -m src.control_group.assignment --experiment-name "experiment_001"

# 对照组效果分析
python -m src.control_group.analysis --experiment-id 1

# 运行单元测试
pytest tests/unit/

# 启动API服务
python src/api/app.py

# 查看日志
tail -f logs/app.log
```

#### 5.4.2 配置文件示例

**config/database.yaml**
```yaml
database:
  host: localhost
  port: 5432
  name: shanjiaxia_db
  user: postgres
  password: ${DB_PASSWORD}
  
retention:
  transactions: 180  # 天
  interactions: 180
  features: 180
  predictions: 365
```

**config/features.yaml**
```yaml
feature_engineering:
  window_days: 20  # 特征窗口期
  
  feature_groups:
    - basic_info
    - rfm
    - transaction
    - interaction
    - trend
    - lifecycle
    - social
    - risk
  
  calculation_schedule:
    cron: "0 2 * * *"  # 每天凌晨2点
```

**config/model.yaml**
```yaml
model:
  type: xgboost
  
  hyperparameters:
    max_depth: 6
    learning_rate: 0.1
    n_estimators: 100
    subsample: 0.8
    colsample_bytree: 0.8
  
  evaluation:
    metrics:
      - auc
      - accuracy
      - precision
      - recall
      - f1
    
    threshold: 0.5
  
  retraining:
    frequency: weekly
    min_samples: 10000
```

**config/control_group.yaml**
```yaml
control_group:
  enabled: true
  control_ratio: 0.10  # 对照组比例
  
  assignment:
    random_seed: 42
    min_risk_score: 0.7  # 只对高风险客户分组
  
  tracking:
    observation_days: 7
    auto_track: true
  
  analysis:
    significance_level: 0.05
    auto_report: true
```

#### 5.4.3 目录快速导航

| 内容 | 文件路径 |
|-----|---------|
| 📊 项目计划 | `projects/山脚下/项目计划.md` |
| 📍 目录导航 | `projects/山脚下/项目计划_目录导航.md` |
| 📈 进度跟踪 | `projects/山脚下/进度跟踪.md` |
| 🚀 快速恢复 | `projects/山脚下/新聊天快速恢复.md` |
| 🗂️ 数据架构 | [第三部分 3.2节](#32-数据架构设计) |
| 🎯 88特征体系 | [第四部分](#第四部分88特征体系) |
| 👥 对照组功能 | [第五部分 5.3节](#53-对照组功能实现) |
| 👨‍💼 员工配置 | [第五部分 5.1节](#51-员工配置与权限) |
| 🗓️ 实施路线 | [第五部分 5.2节](#52-四阶段实施计划) |

#### 5.4.4 核心指标

**数据指标**:
- 数据保留期: 180天
- 特征窗口期: 20天
- 预测周期: 7天
- 特征总数: 88个

**模型指标**:
- 目标AUC: ≥ 0.75
- 目标准确率: ≥ 70%
- 预测任务耗时: < 30分钟

**业务指标**:
- 目标流失率降低: 15-20%
- 挽留成功率: ≥ 40%
- 对照组比例: 10%

#### 5.4.5 重要日期

- 项目启动: 2025-12-17
- 阶段一完成: 2026-01-17 (30天)
- 阶段二完成: 2026-02-17 (60天)
- 阶段三完成: 2026-03-17 (90天)
- 阶段四完成: 2026-04-17 (120天)
- 首次对照组实验: 阶段四第3周

---

## 附录

### A. 技术栈选型

**编程语言**: Python 3.8+

**数据存储**:
- PostgreSQL (主数据库)
- Redis (缓存)

**数据处理**:
- Pandas
- NumPy
- SQLAlchemy

**机器学习**:
- scikit-learn
- XGBoost
- LightGBM
- SHAP (模型解释)

**Web框架**:
- FastAPI (API开发)
- Uvicorn (ASGI服务器)

**任务调度**:
- APScheduler

**监控告警**:
- Prometheus
- Grafana

**版本控制**:
- Git
- GitHub

### B. 依赖包清单

```txt
# requirements.txt

# Data Processing
pandas==1.5.3
numpy==1.24.3
sqlalchemy==2.0.15
psycopg2-binary==2.9.6

# Machine Learning
scikit-learn==1.2.2
xgboost==1.7.5
lightgbm==3.3.5
shap==0.41.0
imbalanced-learn==0.10.1

# Web Framework
fastapi==0.95.2
uvicorn==0.22.0
pydantic==1.10.7

# Task Scheduling
apscheduler==3.10.1

# Configuration
pyyaml==6.0
python-dotenv==1.0.0

# Logging & Monitoring
loguru==0.7.0

# Testing
pytest==7.3.1
pytest-cov==4.1.0

# Jupyter
jupyter==1.0.0
ipykernel==6.23.1

# Utilities
python-dateutil==2.8.2
tqdm==4.65.0
```

### C. Git工作流

**分支策略**:
- `main`: 主分支，保持稳定
- `develop`: 开发分支
- `feature/*`: 功能分支
- `hotfix/*`: 紧急修复分支

**提交规范**:
```
<type>(<scope>): <subject>

<body>

<footer>
```

**Type类型**:
- `feat`: 新功能
- `fix`: Bug修复
- `docs`: 文档更新
- `style`: 代码格式
- `refactor`: 重构
- `test`: 测试
- `chore`: 构建/工具

### D. 文档更新记录

| 版本 | 日期 | 更新内容 | 更新人 |
|-----|------|---------|--------|
| v1.0 | 2025-12-01 | 初始版本 | - |
| v2.0 | 2025-12-17 | 新增对照组功能、调整数据保留期和特征窗口 | - |

---

## 结语

本项目计划书详细描述了"山脚下"客户流失预测系统的完整实施方案，包括：

✅ **核心改动**: 数据保留180天、特征窗口20天、对照组功能  
✅ **系统架构**: 六层目录结构、完整数据架构  
✅ **特征体系**: 88个精心设计的特征  
✅ **实施路线**: 四阶段120天实施计划  
✅ **团队配置**: 4人团队职责分工  

**下一步行动**:
1. 组建项目团队
2. 准备开发环境
3. 启动阶段一任务
4. 建立周例会机制
5. 开始每周进度跟踪

**成功关键因素**:
- 严格按照计划执行
- 保持团队高效沟通
- 注重数据质量
- 持续优化迭代
- 重视对照组实验

让我们从山脚下出发，一步一个脚印，构建出卓越的客户流失预测系统！🚀

---

> **文档版本**: v2.0  
> **最后更新**: 2025-12-17  
> **文档状态**: ✅ 完成  
> **GitHub**: https://github.com/laobai6630-dotcom/cto/  
> **本地路径**: D:\cto\projects\山脚下\项目计划.md
